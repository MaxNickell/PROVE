Image A, Image B   ──►  (1) CV & VLM extraction
                         • objects, attrs
                         • candidate relations
                         • global scene descriptors

                     ──►  (2) LLM consolidation
                         • choose salient attrs/relations
                         • enrich with commonsense (e.g. "heavy ⇒ strenuous")

                     ──►  (3) KB builder
                         • Encode facts in Problog
                         • Import reusable rules: stronger/cleaner/smarter…

Multiple-choice options
   ┌────────┐
A) │ text   │
B) │ text   │ ──► (4) Option-to-logic
C) │ text   │     • LLM maps each answer string → FOL hypothesis ϕ_i
D) │ text   │
   └────────┘
                     ──►  (5) Problog inference
                         • For each ϕ_i compute P(KB ⊨ ϕ_i)

                     ──►  (6) Selection & explanation
                         • pick argmax P
                         • surface proof steps for paper figs
